import 'package:flutter/material.dart';

class AppColors {
  static const Color primaryColor = Color(
    0xFF007BFF,
  ); // Un bleu vif pour les éléments principaux
  static const Color accentColor = Color(
    0xFF6C757D,
  ); // Une couleur secondaire, gris
  static const Color textColor = Color(0xFF343A40); // Couleur de texte sombre
  static const Color errorColor = Color(0xFFDC3545); // Rouge pour les erreurs
  static const Color successColor = Color(0xFF28A745); // Vert pour le succès
  static const Color warningColor = Color(
    0xFFFFC107,
  ); // Jaune pour les avertissements
  static const Color backgroundColor = Color(
    0xFFF8F9FA,
  ); // Couleur de fond légère
  static const Color cardColor = Colors.white; // Couleur de fond des cartes
  static Color blueTagBackground = Colors.blue[100]!;
  static Color blueTagText = Colors.blue[800]!;
  static const Color darkBlue = Color(
    0xFF1A2C5B,
  ); // A darker blue for selected toggle
  static const Color linkColor = Color(0xFF3B82F6);
}
